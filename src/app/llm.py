# TODO [Module 1]: wire up the LLM call
# TODO [Module 1]: return the answer plus token usage so Module 5 can track cost


def generate(prompt: str, model: str | None = None, temperature: float = 0.0) -> dict:
    raise NotImplementedError("Module 1: call the LLM and return answer + token usage")
